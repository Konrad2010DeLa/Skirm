{
  "name": "malros-game-snapshot-server",
  "version": "1.0.0",
  "description": "API and viewer for Malros Skirmish game snapshot uploads",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "netlify:dev": "npm install && netlify dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "netlify-cli": "^22.1.3"
  }
}
